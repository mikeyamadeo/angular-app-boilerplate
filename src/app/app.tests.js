describe('', function() {
    var currentUser;

    beforeEach(module('app'));
    beforeEach(inject(function($injector) {
        // Job = $injector.get('Job');
        // appCtrl = $injector.get('appCtrl');
        // appCtrl = $injector.get('appCtrl');
    }));

    it('Should assign a value to currentUser', function() {
        // expect(appCtrl.currentUser).toBe(null);
        // console.log(Job.name);
        expect(0).toBe(0);
    });
})